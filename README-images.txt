圖片替換建議

1. 目前網站中的灰藍色區塊都是圖片占位區，可直接改成 img 標籤或 CSS 背景圖。
2. 建議圖片整理在同一個資料夾，例如：
   D:\codex\industrial-corporate-site\images\
3. 建議檔名範例：
   home-hero.jpg
   about-factory.jpg
   products-connector.jpg
   technology-line.jpg
   quality-lab.jpg
   contact-office.jpg
4. 若你要，我下一步可以直接幫你把每個占位區改成已連接的真實圖片路徑版本。

[2026-04-02 圖檔已建立]
- 已依照 UPLOAD-GUIDE 的命名規則，在 `images/` 建立 22 個 `.svg` 圖片檔（文字檔，可在 PR 檢視）。
- 六個頁面目前都使用本地路徑 `images/<欄位>.svg`。
- 目前圖檔為示意用途，可直接以同檔名覆蓋為正式圖片（或改回 .jpg）。
- 後續若上傳同名 `.jpg`（與 `.svg` 同名），前端會自動優先顯示 `.jpg`；若無 `.jpg` 則回退顯示 `.svg`。
